# Chiron Analytics v2 - Backend Environment Variables
# Copy to .env and fill in values

# Snowflake (uses existing config.json from parent CHIRON_MONITORING)
# No additional config needed if config.json is present

# Optional: Override API settings
# HOST=0.0.0.0
# PORT=8000
# DEBUG=false
